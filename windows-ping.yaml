---
# This playbook uses the win_ping module to test connectivity to Windows hosts
- name: Ping Windows hosts
  hosts: all
  gather_facts: false
  tasks:
    - name: Run win_ping to verify connectivity and authentication
      ansible.windows.win_ping:
